# Authentication config file

users:
  # "admin" here is username
  admin:
    name: "Admin"
    # SHA-256 hash of password which can be computed with "echo -n password | shasum -a 256"
    # This placeholder string is dynamically replaced in the compose.yml command
    password: "admin-password-hash-placeholder"
